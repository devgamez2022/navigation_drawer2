import 'package:flutter/material.dart';

class BrandonMejia extends StatelessWidget {
  const BrandonMejia({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Archivo Brandon'),
      ),
      body: Column(
        children: [
          Text('Fichero de Brandon')
        ],
      ),
    );
  }
}